# LOCAL SYNTHETIC STACK ONLY. Copy to ignored .env and supply a local password.
DB_HOST=postgres
DB_PORT=5432
DB_NAME=muv_synthetic
DB_USER=muv_synthetic
DB_PASSWORD=
# Synthetic only; staging must supply its own approved external account config.
MUV_BANK_ACCOUNT_ID=TEST_BANK_ACCOUNT_001
